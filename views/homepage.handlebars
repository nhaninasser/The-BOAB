{#unless posts}}
    <p class="display-message">No posts yet. Sign up and make the first post on The BOAB(Best of all Blogs!</p>
{{/unless}}
<ul>
    {{> post-info post}}
    {{#each posts as |post|}}
    <li>
        <a href="/post/{{post.id}}">
            {{> post-info post}}
        </a>
    </li>
    {{/each}}
</ul>